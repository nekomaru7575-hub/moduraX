Rewrite the JavaScript function below according to the instructions.

Output rules (strict):
- Output ONLY the rewritten function, wrapped in a ```js code fence.
- Do NOT write any explanation, preamble, or trailing notes.
- Do NOT change the function name.
- Keep the existing code style: same indentation, same comment language, same naming conventions.
- Change only what the instructions ask for. Do not refactor, reformat, or "improve" anything else.

Function:
```js
{{FUNCTION}}
```

Instructions:
{{INSTRUCTION}}
